/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['localhost', 'flowbite.s3.amazonaws.com', 'flowbite-demo.vercel.app'],
    }
}

module.exports = nextConfig
